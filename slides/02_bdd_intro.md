# Behavior-Driven Development

Define behavior before you build.

Given a context —
When something happens —
Then the system reacts.

Human-readable. Machine-executable. Shared language.

**The spec is the source code for the source code.**
